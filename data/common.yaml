---

lookup_options:
  ducktape::firewall::rules:
    merge:
      strategy: deep
      knockout_prefix: --
  ducktape::firewall::chains:
    merge:
      strategy: deep
      knockout_prefix: --
  ducktape::haproxy::autoload::frontends:
    merge:
      strategy: deep
      knockout_prefix: --
  ducktape::haproxy::autoload::backends:
    merge:
      strategy: deep
      knockout_prefix: --
  ducktape::haproxy::autoload::userlists:
    merge:
      strategy: deep
      knockout_prefix: --
  ducktape::jenkins::autoload::cli_execs:
    merge:
      strategy: deep
  ducktape::jenkins::autoload::credentials:
    merge:
      strategy: deep
  ducktape::jenkins::autoload::native_credentials:
    merge:
      strategy: deep
  ducktape::jenkins::autoload::users:
    merge:
      strategy: deep
  ducktape::ldap::autoload::entries:
    merge:
      strategy: deep
      knockout_prefix: --
  ducktape::logstash::configfiles:
    merge:
      strategy: deep
      knockout_prefix: --
  ducktape::rsyslog::autoload::snippets:
    merge:
      strategy: deep
      knockout_prefix: --

ducktape::firewall::rules::defaults: {}
ducktape::firewall::rules: {}
ducktape::firewall::chain::defaults: {}
ducktape::firewall::chains: {}
